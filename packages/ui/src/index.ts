/**
 * @stock-brain/ui
 *
 * Shared UI components for Stock Brain.
 *
 * Components added here should be purely presentational — no business logic,
 * no data fetching. All data and calculations arrive as props from the app layer,
 * which in turn derives them from @stock-brain/domain.
 *
 * This package is intentionally empty in Phase 0.
 * UI components will be added in Phase 6 (UI + print engine).
 */

export {}

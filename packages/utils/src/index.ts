/**
 * @stock-brain/utils
 *
 * Shared utility functions for Stock Brain.
 * Pure functions only — no business logic, no side effects, no domain knowledge.
 *
 * Good candidates: date formatting, number rounding helpers, string normalization.
 * Bad candidates: stock calculations, dispatch validation — those belong in @stock-brain/domain.
 *
 * This package is intentionally empty in Phase 0.
 */

export {}
